# .protocol.md — Session Manifest

curator_mode: auto
session_log: DESIGN.md
entry_point: CLAUDE.md
# memory: comma-separated adapter ids, or omit to auto-discover all.
#   ids: claude-native, agents-md, cline-bank, copilot, cursor, ijfw
memory:
# adoption: how Curator was added. link = point at existing docs (Curator owns
#   only this file; revert = delete it). convert = additively upgraded — see .curator/.
adoption: link

## Project
name:
description:

## Current Focus
branch:
sprint:
active:

## Key Commands
# build:
# test:
# deploy:

## Session Notes
